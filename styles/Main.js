import { StyleSheet } from "react-native";

const main = StyleSheet.create({
    backgroungScreens: {
        backgroundColor: '#08074F'
    },
    mainText: {
        color: '#FFFFFF'
    }
})

export default main